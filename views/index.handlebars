<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao blog do NodeJs!</h1>
  <p class="lead">Esse é um blog desenvolvido em Node.js, Express, MongoDB e Handlebars.</p>
  <small>Utilizando Sessões, Middlewares e o pacote Passport para controle de cadastro e login.</small><br>
  <small>Criptografia(Hash) de senha através do pacote Bcryptjs.</small>
  <hr class="my-4">
  <p>Desenvolvido por Sidney.</p>
  <a class="btn btn-success btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <hr>
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-primary">Leia mais</button></a>
        <hr>
        <small><b>Categoria:</b> {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}